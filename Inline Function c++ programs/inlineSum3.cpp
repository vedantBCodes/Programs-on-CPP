#include<iostream>
using namespace std;
inline void sum(int a,int b)
{
    cout<<"Sum:-"<<a+b;
}
int main()
{
    sum(4,6);
    //If a function doesn't returns anything then there we can call that function directly without using cout
}

